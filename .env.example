NODE_ENV=development
PORT=3000

# PostgreSQL (Docker)
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
POSTGRES_DB=your_db_name
POSTGRES_PORT=5432

# Used by drizzle-kit locally (points to localhost)
# In Docker, this is overridden by docker-compose with the service hostname
DATABASE_URL=postgresql://your_db_user:your_db_password@localhost:5432/your_db_name

# JWT — change in production (minimum 32 chars)
JWT_SECRET=change_this_to_a_random_secret_minimum_32_characters
JWT_EXPIRES_IN=7d

# OTP expiry in minutes
OTP_EXPIRES_IN_MINUTES=10

# CORS — comma-separated list of allowed origins
ALLOWED_ORIGINS=http://localhost:3001
